import styled from 'styled-components';

const SWrapper = styled.div`
	margin: 0 auto;
	width: 20rem;
`;

const SForm = styled.form`
	display: flex;
	flex-direction: column;
`;
export { SWrapper, SForm };
